{ ... }:
{
  # Host-specific configuration for personal MacBook
  # Common darwin config is imported via modules/darwin/common.nix in flake.nix
  
  # Add any personal MacBook specific overrides here
  # For example, different homebrew packages can be added:
  # homebrew.casks = [ "some-personal-app" ];
}
